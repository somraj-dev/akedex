{
  "name": "acadex-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:desktop": "npm run dev:desktop --prefix frontend",
    "package": "npm run package --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
